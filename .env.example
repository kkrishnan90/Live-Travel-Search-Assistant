# create your own API KEY at https://aistudio.google.com/apikey
REACT_APP_GEMINI_API_KEY=''
REACT_APP_DISCOVERY_ENGINE_ACCESS_TOKEN=''

# Configuration for the Discovery Engine Search Tool
# URL for the Discovery Engine search API endpoint
REACT_APP_SEARCH_HOTEL_TOOL_URL='https://discoveryengine.googleapis.com/v1alpha/projects/<PROJECT_NUMBER>/locations/global/collections/default_collection/engines/<VERTEX_AI_SEARCH_APP_ID>//servingConfigs/default_search:search'
# JSON definition of the Discovery Engine search tool for Gemini
REACT_APP_SEARCH_HOTEL_TOOL_DEFINITION='{"name":"search_discovery_engine","description":"Searches the internal Traveloka hotel database for specific hotel information like name, price, rating, amenities, and availability based on user queries like location, dates, or hotel name.","parameters":{"type":"OBJECT","properties":{"query":{"type":"STRING","description":"The search term or query to use for finding hotels."}},"required":["query"]}}'

# --- Gemini Model Configuration ---
# The name of the Gemini model to use for the Live API
REACT_APP_MODEL_NAME="models/gemini-2.0-flash-exp"

# --- Gemini Generation Configuration ---
# JSON string defining generation parameters (e.g., response modality, voice)
REACT_APP_GENERATION_CONFIG='{"responseModalities":"audio","speechConfig":{"voiceConfig":{"prebuiltVoiceConfig":{"voiceName":"Aoede"}}}}'

# --- Gemini System Instructions ---
# The initial instructions given to the Gemini model
REACT_APP_SYSTEM_INSTRUCTIONS='You are a helpful travel assistant. When asked specifically to search for hotels (e.g., finding hotels in a location, checking availability, getting prices, amenities), use *only* the \'search_discovery_engine\' function. For other general web searches (e.g., news, facts, topics not related to specific hotel bookings), use the Google Search tool.'

